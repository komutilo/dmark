const {
  getConfig,
  getLabels,
  getStacks,
  getStages,
  getRunner,
  executeCommand,
} = require('./dmark');

module.exports = {
  getConfig,
  getLabels,
  getStacks,
  getStages,
  getRunner,
  executeCommand,
};
